---
description: 此 Vue 3 + TypeScript 專案的核心開發規範
alwaysApply: true
---

# 專案核心規範

## 技術棧與範圍
- 本專案使用 Vue 3 SFC、TypeScript 與 Vite。
- 優先修改既有檔案與既有模式，避免建立平行實作。

## 程式風格
- 遵循相鄰檔案既有的格式與命名風格。
- 變更保持精簡，並聚焦在需求行為本身。
- 除非有明確要求，避免大型重構。

## TypeScript 與 Vue
- 維持嚴格 TypeScript 型別安全；非必要不要使用 `any`。
- 元件邏輯需清楚且就近放置；僅在可重用時才抽離 helper。
- `prop`、`emit` 與 composable 命名需具描述性。

## 品質檢查門檻
- 進行較大改動後需執行相關檢查（`npm run lint`，必要時包含 build）。
- 在完成前修復本次新增的 lint 或型別錯誤。
- 不要為了格式偏好去修改不相關檔案。

## 依賴與架構規則
- 新增套件前先優先重用既有依賴與既有實作模式。
- 若必須新增套件，需說明原因並將變更範圍控制在最小。
- 避免在應用啟動流程或共享模組中引入全域副作用。

## Git 與安全原則
- 除非使用者明確要求，否則不要自行 commit 或 push。
- 不要回退與本次任務無關的使用者變更。
