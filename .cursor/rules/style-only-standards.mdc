---
description: 專案樣式規範（僅針對 CSS 與 Vue style）
alwaysApply: true
---

# 樣式專用規範

## 適用範圍
- 本規則只針對樣式內容（CSS 與 Vue `<style>` 區塊）。
- 不處理業務邏輯、資料流或 API 行為。

## 技術限制
- 一律使用原生 CSS。
- 禁止使用 SCSS / Sass（包含 `lang="scss"`、`@use`、`@mixin`、巢狀語法）。

## 撰寫原則
- 優先沿用既有 class 命名與版面模式，避免重複定義。
- 選擇器維持可讀且可維護，避免過深層級與過度耦合。
- 樣式異動最小化，只改需求涉及區塊。

## 命名策略（@scope / scoped）
- 在 `@scope` 或 Vue scoped style 情境下，不強制使用 BEM 命名。
- template 可使用語意化、簡潔的 class（例如 `content`、`title`、`store`）。
- 只有在跨區塊共用、或容易產生命名衝突時，才考慮更長命名或 BEM。

## 設計一致性
- 優先重用既有色彩、字體、間距與圓角設定。
- 相同視覺語意使用相同樣式規則，不重複造輪子。
- 需要新增樣式時，先確認是否可抽成可重用 class。

## 品質檢查
- 完成樣式調整後，檢查畫面在常見尺寸下是否維持可讀性與排版穩定。
- 不為了樣式任務去修改不相關檔案或重排整份檔案格式。
